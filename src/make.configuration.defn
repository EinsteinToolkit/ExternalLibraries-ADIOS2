# make.configuration.defn file for thorn ADIOS

# Define standard ADIOS utilities and install those that we could build/that are
# present in the system
STD_ADIOS_UTILS = 
ALL_UTILS += $(STD_ADIOS_UTILS)
