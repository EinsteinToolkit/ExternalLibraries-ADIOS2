# Main make.code.deps file for thorn ADIOS2

export ADIOS2_INSTALL_DIR ADIOS2_ENABLE_SST ADIOS2_ENABLE_FORTRAN ADIOS2_ENABLE_CUDA
export CMAKE_DIR
export HAVE_CAPABILITY_MPI HAVE_CAPABILITY_CUDA

$(CCTK_TARGET) $(OBJS) $(SRCS:%=%.d): $(SCRATCH_BUILD)/done/$(THORN)

ifneq ($(ADIOS2_BUILD),)
$(SCRATCH_BUILD)/done/$(THORN): $(SRCDIR)/build.sh
	+$(SRCDIR)/build.sh
endif
